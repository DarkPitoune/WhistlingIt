# whistling.it
#
# Everything is public and there is nothing to hide from a crawler, so this file
# exists to point at the sitemap rather than to restrict anything. Worth knowing:
# an absent robots.txt already means "crawl everything", so this does not by
# itself make the site rank — it just makes the sitemap discoverable and stops a
# 404 showing up in Search Console.

User-agent: *
Allow: /

# The two language roots, /fr and /en, are real files the deploy workflow writes,
# so they answer 200 and are the only two URLs in the sitemap. Everything below
# them (/fr/booth, /en/calendar, /fr/2026-09-02, …) is client-side routing that
# GitHub Pages serves as 404.html — the app renders fine but the status is 404,
# so they stay out of the sitemap; do not add them until the hosting can return
# 200 there too.

Sitemap: https://whistling.it/sitemap.xml
